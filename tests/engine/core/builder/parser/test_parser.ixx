export module test_parser;

import std;
import parser;
import test_core;

using namespace ge::test_core;
using namespace ge::test_core::assert;

UNIT_TEST(parser, class_name)
{
	ge::parser parser{};
}